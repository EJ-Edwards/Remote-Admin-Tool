# Sentinel Link — Dashboard UI

Vite + React + Tailwind B2B dashboard.

## Development

```bash
npm install
npm run dev
```

Set `VITE_API_URL=http://localhost:8000` in `.env` if needed.

## Docker

Started via `docker compose` from `cloud-dashboard/`.
